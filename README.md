# セットアップ方法


## 必要条件

- [Node.js](https://nodejs.org/)（推奨バージョン: 最新のLTS）
- npm（Node.jsに同梱）

## セットアップ手順（ルートで）

1. 依存パッケージをインストールします。

    ```bash
    npm install
    ```

2. 開発サーバーを起動します。

    ```bash
    npm run dev
    ```

3. Live Serverではなく，2で起動したポートにアクセスしてください
